function scrollToTop() {
    window.scrollTo({
        top: 0,
        behavior: 'smooth'
    });
}

function search_button() {
	if (!search_bar.text.value) {
		alert("검색어를 입력해주세요");
		search_bar.text.focus();
		return;
	}
	
	search_bar.submit();
}